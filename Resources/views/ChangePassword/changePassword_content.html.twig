<form action="{{ path('events_user_change_password') }}" {{ form_enctype(form) }} method="POST" class="events_user_change_password">
    {{ form_widget(form) }}
    <div>
        <input type="submit" value="{{ 'change_password.submit'|trans({}, 'EventsUserBundle') }}" />
    </div>
</form>
